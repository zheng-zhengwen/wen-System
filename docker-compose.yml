services:
  app:
    build: .
    container_name: awenops
    restart: unless-stopped
    ports:
      - "${PORT:-8080}:80"
    volumes:
      - awenops-data:/app/data
    env_file:
      - .env
    environment:
      - AWENOPS_DATA_DIR=/app/data

volumes:
  awenops-data:
    driver: local
