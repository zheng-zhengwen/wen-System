# 安全策略

## 报告漏洞

**请不要开公开 issue。** 发到 **crushxue@outlook.com**，标题带 `[security]`。

承诺：**72 小时内首次回复**。修复后会在 release notes 里致谢（你也可以选择匿名）。

报告里有这些会快很多：影响的版本、复现步骤、你判断的影响面。如果方便，附上诊断包（设置页可导出，不含密钥和店铺数据）。

---

## 这个产品的威胁模型

awenops 是**自托管**的：它跑在你自己的机器上，数据也在你自己的机器上。所以边界和 SaaS 不一样，说清楚免得误判。

### 我们防的

- **子进程偷凭据**。启动时会把 awenops 自己的凭据从 `os.environ` 里摘走（`core/secret_env.py`），高危路径（终端、AI 工具调用）的子进程环境再单独剥一层（`core/proc.py`）。否则终端里一条 `env` 命令就能看到会话签名密钥。
- **配置文件里的明文密钥**。API 密钥在磁盘上是 AES-256-GCM 加密的（`core/secrets.py`）。代码是公开的，明文存储等于把"去哪儿捞"也一并公开了。
- **越权**。被授予某个板块的普通成员，拿不到管理员才有的整机能力（浏览任意目录、读项目外的文件、在任意位置建项目）。
- **备份里的凭据**。备份包里的密钥是密文；主密钥只在你设了备份口令时才随包走，且是加密的。
- **诊断包泄密**。导出的诊断包不含密钥原文、不含店铺数据，且导出过程**不发起任何网络请求**。

### 我们不防的（也防不了）

- **已经拿到机器 root 的攻击者**。主密钥和密文放在同一台机器上；再往上就得要求每次启动手输密码，那会毁掉开机自启。
- **管理员自己**。管理员有内置终端，本来就有整机权限。管理员账号 = 机器权限，请据此分配。
- **你装的第三方 Skill 和 MCP server 的行为**。它们跑在你的机器上。Skill 中心里的内容**未经官方审计**，装之前请看清楚它要做什么。

---

## 部署建议

- **别用 root 跑**。`setup.sh` 会建专用用户；`deploy/systemd/` 下的模板已带最小权限设置。
- **别把 8001 直接暴露公网**。默认绑 `127.0.0.1`，要外网访问请走 nginx + HTTPS + 强密码。
- **`.env` 里的 `AWENOPS_SECRET` 要设成固定值并保密**。它是会话签名密钥 —— 泄漏等于别人可以伪造管理员会话。
- **备份记得带口令**，否则 `data/.master.key` 这个隐藏文件一漏，所有密钥都解不开。

---

## 支持的版本

只对**最新发行版**提供安全修复。这是个快速迭代的自托管项目，请保持更新。
